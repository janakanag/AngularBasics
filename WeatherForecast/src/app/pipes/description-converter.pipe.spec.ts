import { DescriptionConverterPipe } from './description-converter.pipe';

describe('DescriptionConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
